Claude Code lets you use Sonnet 4.5 with a 1M-token context window on the Max
plan, which is great for working across a large codebase in one session. You
can install MCP servers to connect Claude Code to external tools, and once
configured they show up automatically in every new session.
